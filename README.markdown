# Generate a CSV table with date column.

This is a sample application that generates a CSV table with a date column.
Each row increments by one hour from the previous.

For usage instructions, type `python program.py -h`.

Example

```shell
python program.py out.csv 1900-01-01 2022-01-01
```
